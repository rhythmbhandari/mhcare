import 'package:flutter/material.dart';

class AddDiagnosisScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Diagnosis'),
      ),
      body: Center(
        child: Text('Add Diagnosis form goes here'),
      ),
    );
  }
}
